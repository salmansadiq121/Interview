/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "www.google.com",
      "encrypted-tbn0.gstatic.com",
      "res.cloudinary.com",
    ],
  },
};

export default nextConfig;
